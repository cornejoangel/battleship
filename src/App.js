import React, { useState } from 'react';
import Grid from './components/Grid';
import './styles/normalize.css';

const App = () => {
  const x = <Grid player={1} name="home" />;
  return x;
};

export default App;
